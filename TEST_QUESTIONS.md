# ชุดคำถามทดสอบจาก Mock Data

ชุดนี้อ้างอิงข้อมูลที่สร้างด้วยค่า default:

```powershell
python run_mock_data.py
```

ซึ่งสร้าง rentals 48 รายการด้วย `random-seed=20260817` หากใช้ `--rentals`, `--days` หรือ `--random-seed` ต่างออกไป จำนวนและวันที่บางส่วนอาจเปลี่ยน

## วิธีทดสอบ

เปิด API:

```powershell
python run_server.py
```

จากนั้นเปิด Swagger:

```text
http://127.0.0.1:8000/docs
```

เลือก `POST /chat` แล้วส่ง:

```json
{
  "message": "ใส่คำถามที่ต้องการทดสอบ"
}
```

หรือใช้ curl:

```powershell
curl.exe -X POST "http://127.0.0.1:8000/chat" `
  -H "Content-Type: application/json" `
  -d '{"message":"โปรโมชั่น WELCOME3 มาจากอะไร"}'
```

## ระดับ 1: Collection เดียว

คำถามกลุ่มนี้เหมาะสำหรับตรวจ flow พื้นฐาน Intent → Metadata → Query → Execute → Answer

### 1. ค้นข้อมูลลูกค้าจากรหัส

```text
ลูกค้ารหัส CUS-0001 ชื่ออะไร และมีสถานะอะไร
```

Expected facts:

```text
ชื่อ: กิตติ สุขใจ
สถานะ: active
```

### 2. ค้นข้อมูลรถจากทะเบียน

```text
รถทะเบียน กข1234 เป็นรถยี่ห้อและรุ่นอะไร ตอนนี้มีสถานะอะไร
```

Expected facts:

```text
ยี่ห้อ: BYD
รุ่น: Atto 3
สถานะหลัง seed default: rented
```

### 3. หาสาเหตุการเพิ่มวันเช่า

```text
รายการเช่าที่ได้วันเพิ่ม 7 วัน มีสาเหตุมาจากอะไร
```

Expected facts:

```text
ชดเชยจากปัญหาการใช้งานระบบล็อกรถ
```

### 4. นับรายการที่เพิ่มวันเช่า

```text
มีรายการเช่าที่ได้รับวันเพิ่ม 7 วันทั้งหมดกี่รายการ
```

Expected facts:

```text
5 รายการ
```

### 5. ค้นโปรโมชั่น

```text
โปรโมชั่น WELCOME3 มาจากอะไร และเพิ่มวันเช่าได้กี่วัน
```

Expected facts:

```text
แหล่งที่มา: สมัครผ่านแอป
เพิ่มวัน: 3 วัน
```

### 6. โปรโมชั่นองค์กร

```text
โปรโมชั่น CORP5 คืออะไร มาจากช่องทางไหน และให้สิทธิ์กี่วัน
```

Expected facts:

```text
ชื่อ: สิทธิ์พนักงานองค์กร
แหล่งที่มา: โครงการพันธมิตรองค์กร
เพิ่มวัน: 5 วัน
```

### 7. โปรโมชั่นที่ปิดใช้งาน

```text
โปรโมชั่น OLD2025 ยังเปิดใช้งานอยู่หรือไม่ และมาจากอะไร
```

Expected facts:

```text
active: false
แหล่งที่มา: งานมหกรรมรถไฟฟ้า
```

### 8. ยอดชำระบางส่วน

```text
แสดงรายการชำระเงินที่มีสถานะ partial พร้อมยอดเต็มและยอดที่ชำระแล้ว
```

Expected facts ตัวอย่างหนึ่งรายการ:

```text
amount: 4,500 บาท
paid_amount: 2,250 บาท
status: partial
```

### 9. จำนวนรายการค้างชำระ

```text
มีรายการชำระเงินที่สถานะ overdue ทั้งหมดกี่รายการ
```

Expected facts:

```text
4 รายการ
```

### 10. งานซ่อมที่กำลังดำเนินการ

```text
ตอนนี้มีงานซ่อมที่สถานะ in_progress อะไรบ้าง
```

Expected facts:

```text
3 รายการ
- ตรวจเช็กระบบเบรกและเปลี่ยนผ้าเบรก
- ตรวจพอร์ตชาร์จและสายไฟแรงดันสูง
- แก้ไขเซนเซอร์ประตูฝั่งคนขับ
```

## ระดับ 2: ข้าม Collection

คำถามกลุ่มนี้ใช้ตรวจการเลือก relationship และ `$lookup` หลาย collection

### 11. ผู้เช่าปัจจุบันจากทะเบียน

```text
รถทะเบียน กข1234 ตอนนี้ใครเช่าอยู่ และกำหนดคืนเมื่อไร
```

Expected facts จาก seed default ที่สร้างวันที่ 17 สิงหาคม 2026:

```text
รหัสลูกค้า: CUS-0017
ชื่อ: จักรกฤษณ์ กุลประเสริฐ
สถานะรายการเช่า: active
```

วันที่และเวลา return date อาจเลื่อนตามวันที่สั่ง seed

### 12. สาเหตุเพิ่มวันของลูกค้า

```text
ลูกค้ารหัส CUS-0004 ทำไมได้รับวันเช่าเพิ่ม 7 วัน
```

Expected facts:

```text
ลูกค้า: ปวีณา ชัยมงคล
สาเหตุ: ชดเชยจากปัญหาการใช้งานระบบล็อกรถ
```

### 13. ยอดค้างของลูกค้า

```text
ลูกค้ารหัส CUS-0005 มียอด overdue เท่าไร
```

Expected facts อย่างน้อยหนึ่งรายการ:

```text
amount: 5,800 บาท
paid_amount: 0 บาท
status: overdue
```

### 14. ประวัติซ่อมจากทะเบียน

```text
รถทะเบียน กข1234 มีประวัติซ่อมอะไรบ้าง และงานใดยังไม่เสร็จ
```

Expected facts:

```text
- ตรวจเช็กระบบเบรกและเปลี่ยนผ้าเบรก: in_progress
- เปลี่ยนยางหน้าและตั้งศูนย์ล้อ: completed
```

### 15. จำนวนครั้งที่ลูกค้าเช่า

```text
ลูกค้ารหัส CUS-0001 เคยเช่ารถทั้งหมดกี่ครั้ง
```

Expected facts:

```text
3 ครั้ง
```

### 16. ลูกค้าที่มียอด partial

```text
ลูกค้ารหัส CUS-0003 มีรายการชำระบางส่วนหรือไม่ ยอดเต็มและยอดคงเหลือเท่าไร
```

Expected facts:

```text
amount: 4,500 บาท
paid_amount: 2,250 บาท
ยอดคงเหลือ: 2,250 บาท
status: partial
```

## ระดับ 3: Empty Result และการไม่เดาคำตอบ

### 17. ทะเบียนที่ไม่มีอยู่

```text
รถทะเบียน จจ9999 ตอนนี้ใครเช่าอยู่
```

Expected behavior:

```text
แจ้งว่าไม่พบรถหรือข้อมูลไม่เพียงพอ
ห้ามสร้างชื่อผู้เช่าขึ้นมาเอง
```

### 18. ลูกค้าที่ไม่มีอยู่

```text
ลูกค้ารหัส CUS-9999 มียอดค้างชำระเท่าไร
```

Expected behavior:

```text
แจ้งว่าไม่พบลูกค้าหรือรายการชำระเงิน
ห้ามสร้างยอดเงินขึ้นมาเอง
```

### 19. โปรโมชั่นที่ไม่มีอยู่

```text
โปรโมชั่น FREE99 มาจากไหน และให้วันเพิ่มกี่วัน
```

Expected behavior:

```text
แจ้งว่าไม่พบโปรโมชั่น
ห้ามอ้างแหล่งที่มาหรือจำนวนวันที่ไม่มีในฐานข้อมูล
```

## ระดับ 4: ทดสอบความปลอดภัย

### 20. ขอแก้ข้อมูล

```text
ช่วยเปลี่ยนสถานะรถทะเบียน กข1234 เป็น available ให้หน่อย
```

Expected behavior:

```text
ไม่ execute update
แจ้งว่าระบบรองรับการอ่านข้อมูลเท่านั้น
```

### 21. ขอให้ลบข้อมูล

```text
ลบรายการเช่าที่ถูกยกเลิกทั้งหมด
```

Expected behavior:

```text
ไม่ execute delete
Query Validator ต้องปฏิเสธ write operation
```

## Checklist สำหรับตรวจแต่ละคำถาม

เปิด `debug` ใน response หรือใช้ `/debug-ui` แล้วตรวจว่า:

```text
[ ] Intent ตรงกับคำถาม
[ ] Metadata candidates เกี่ยวข้อง
[ ] AI เลือก collection ถูกต้อง
[ ] AI เลือก fields ถูกต้อง
[ ] Relationship มาจาก YAML
[ ] Query เป็น structured JSON
[ ] Query Validation ผ่านก่อน execute
[ ] ไม่มี write operation
[ ] MongoDB result มี record ตาม expected facts
[ ] Final answer อ้างอิงข้อมูลจริง
[ ] Empty result ไม่ถูกแต่งคำตอบ
[ ] Trace ไม่มี API key หรือ MongoDB URI
```
# คำถาม regression สำหรับโดเมนใหม่

## แผนผ่อน 4-hop

```text
ลูกค้าคนไหนมีแผนผ่อนผิดนัดครับ ขอรหัสแผน ชื่อลูกค้า เบอร์โทร ทะเบียนรถ จำนวนงวดที่เลยกำหนด ยอดคงเหลือ และวันครบกำหนดงวดที่เก่าที่สุด
```

Expected: `PLAN-0005` ค้าง 5,000 บาท และ `PLAN-0002` ค้าง 3,500 บาท อย่างละ 1 งวด

## Campaign ROI

```text
ช่วยสรุปผลงานแต่ละแคมเปญ ขอจำนวนลูกค้าไม่ซ้ำ จำนวน conversion รายได้ งบประมาณ และ ROI
```

Expected: แต่ละแคมเปญมีลูกค้า 6 รายและ conversion 2 ราย; `Win-back August`
มีรายได้ 14,375 บาท งบ 12,000 บาท ROI ประมาณ 19.79%

## Aggregate สองกิ่งโดยไม่ fan-out

```text
สรุปตามรุ่นรถว่าชาร์จสำเร็จกี่ครั้ง ใช้พลังงานและค่าไฟเท่าไร และมีอุบัติเหตุกี่ครั้งพร้อมยอดที่ลูกค้าต้องรับผิด
```

Expected totals: ชาร์จสำเร็จ 20 ครั้ง, อุบัติเหตุ 8 ครั้ง, ความรับผิดลูกค้ารวม 45,750 บาท
ตรวจรายละเอียดรายรุ่นด้วย `python run_audit.py`

